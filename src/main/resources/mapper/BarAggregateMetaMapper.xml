<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quant.stock.mapper.BarAggregateMetaMapper">

    <select id="selectSourceMaxTime" resultType="java.time.LocalDateTime">
        SELECT source_max_time
        FROM bar_aggregate_meta
        WHERE stock_code = #{stockCode} AND period = #{period}
    </select>

    <insert id="upsertMeta">
        INSERT INTO bar_aggregate_meta (stock_code, period, last_agg_time, source_max_time)
        VALUES (#{stockCode}, #{period}, #{lastAggTime}, #{sourceMaxTime})
        ON DUPLICATE KEY UPDATE
            last_agg_time = VALUES(last_agg_time),
            source_max_time = VALUES(source_max_time),
            updated_at = CURRENT_TIMESTAMP
    </insert>
</mapper>
