---
description: 实质性改动后须检查并更新「应用说明 → 应用待办」
alwaysApply: true
---

# 同步更新应用待办

对本仓库做任何**实质性改动**后，同一轮内必须检查页面「应用说明 → **应用待办**」是否需要更新：

定位：`src/main/resources/static/docs/memo.html`（侧栏由 `stock-chart.js` 的 `knowledgeTopics` 索引加载；菜单标题为「应用待办」）

## 何时必须更新应用待办

- 新增/接入/下线一类能力或数据（复权、资金流、财报、ST、舆情、券商 API 等）
- 开仓过滤、市值/流动性、停牌规则与数据源变化
- 落地或关闭「本地已落地」或「依赖外部 API」中的任一项
- 策略从纯价量扩展到基本面/事件/另类因子时
- 定时任务真实能力变化（已实现 / 桩 / 待 API）

纯文案润色、与路线图无关的小修可不改应用待办。

## 更新要求

1. 「本地已落地」与「依赖外部 API」分区：状态与代码现状一致
2. 不要删掉尚未落地的规划项（本页是整应用路线图）
3. 与 `README.md`、`id: 'app'`、`id: 'rules'` 的同步规则并行：见 `sync-readme.mdc`

## 自检

完成前确认：打开「应用说明 → 应用待办」，状态与当前代码能力一致。
