---
description: 实质性改动后须检查并更新「应用说明 → 数据待办」
alwaysApply: true
---

# 同步更新数据待办（数据与待办）

对本仓库做任何**实质性改动**后，同一轮内必须检查页面「应用说明 → **数据待办**」是否需要更新：

定位：`src/main/resources/static/docs/memo.html`（侧栏由 `stock-chart.js` 的 `knowledgeTopics` 索引加载）

## 何时必须更新数据待办

- 新增/接入/下线一类数据（复权、资金流、财报、ST、舆情等）
- 开仓过滤、市值/流动性、停牌规则与数据源变化
- 落地或关闭数据待办「落地优先级」中的任一项
- 策略从纯价量扩展到基本面/事件/另类因子时

纯文案润色、与数据路线图无关的小修可不改数据待办。

## 更新要求

1. 顶部「落地优先级」列表：状态用 **未落地 / 部分 / 已落地**，与代码现状一致
2. 对应章节表格：若某数据项已实现，在「策略用法/价值」列补一句「已实现：…」或调整表述
3. 不要删掉尚未落地的规划项（数据待办是路线图，不是仅现状说明）
4. 与 `README.md`、`id: 'app'`、`id: 'rules'` 的同步规则并行：见 `sync-readme.mdc`

## 自检

完成前确认：打开「应用说明 → 数据待办」，优先级状态与当前代码能力一致。
