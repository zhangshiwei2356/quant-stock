---
description: git 提交与推送时必须使用清楚的中文备注
alwaysApply: true
---

# Git 提交 / 推送：中文备注必填

用户要求 **git commit** 或 **git push**（含「提交并推送」）时，必须遵守：

## 硬性要求

1. **备注必须中文**：`git commit` 的 message 主体用中文把改动说明清楚；禁止只有英文、或只有模糊词（如 `update` / `fix` / `wip`）。
2. **先说清再推送**：未写清中文说明前，不得 `git push`。
3. **说明要具体**：写清「修了什么 / 加了什么 / 为何改」，避免空话。

## 推荐格式

```
<类型>: <一句话中文摘要>

<可选正文：2～4 行，补充范围、影响面、文档是否已同步>
```

类型可用中英混排前缀（便于扫日志），摘要与正文必须中文，例如：

- `fix: 停牌日拒绝挂卖成交，避免虚假撮合`
- `feat: 组合回测成交时按仓位系数缩量`
- `docs: 同步交易规则与能力与待办中的路径对齐说明`

## 反例

```
# ❌ 不可接受
fix stuff
update
WIP
fix: harden ledger
```

```
# ✅ 可接受
fix: 本地路径尾巴对齐——成交缩量、停牌拒卖、锁忙与ST强平

组合/实盘买单成交时按当前仓位系数缩量；停牌量≤0拒卖；
sync/settle 手动执行锁忙 failLoud。同步 README 与交易规则。
```

## 与其它约定的关系

- 仍须遵守「仅在用户明确要求时才 commit / push」；本规则约束的是**一旦执行时的备注语言与清晰度**。
- 不因此放宽 hooks、`--no-verify`、force push 等安全限制。
