---
description: 禁止新建功能分支，始终在当前检出分支上改动
alwaysApply: true
---

# 在当前分支改动（禁止新建分支）

对本仓库做任何代码/文档改动时：

1. **不要**执行 `git checkout -b` / `git switch -c` 新建功能分支
2. **不要**为云端任务自动创建 `cursor/...` 分支或强制走 PR 分支流程
3. 直接在**当前检出分支**上修改、`git add`、`git commit`、`git push`（推送到当前分支对应远程）
4. 仅当用户**明确要求**另开分支或开 PR 时，才创建分支 / 提 PR

与「仅在用户明确要求时才 commit/push」仍同时生效：用户未要求提交时不要擅自 commit。
