# 宽睿联调 · 左栏收紧 + 接口介绍弹层

日期：2026-08-08  
状态：已批准（用户确认轻量弹层 + 左窄右宽）

## 目标

1. OES / MDS / 账号 / 报撤点测页：左侧接口列表变窄、卡片更紧凑，右侧入参/出参更宽。
2. 每个接口增加「介绍」：轻量浮层展示对齐「宽睿文档梳理」的摘要；可跳转完整文档。

## 交互

- 左卡：「介绍」（次要）+「调用」（主按钮）；点介绍不发起请求。
- 弹层：标题（中文名 · SDK）、摘要正文、底部「打开完整宽睿文档」/「关闭」。
- Esc / 点遮罩关闭；打开完整文档时关闭弹层并 `openKnowledge('kuangrui')`。

## 实现边界

- 前端：`stock.html` 共用弹层；`stock-chart.js` 摘要映射 + 建卡；`style.css` 布局与弹层样式。
- 不改后端 API；保留全部现有 id / 调用路径。
- 同步 README / `app.html` / `memo.html` 一句说明。
